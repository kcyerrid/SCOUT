The following Python modules are required to perform the activity outside of the Obsidian interface:  To install, open a command prompt / terminal window and enter

```bash
pip install feedparser requests readability-lxml beautifulsoup4 python-pptx tkinterdnd2
```